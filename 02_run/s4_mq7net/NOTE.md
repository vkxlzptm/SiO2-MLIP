# S4 — 7net 자체 melt-quench (덱 6p 'Next step' 실행)

## 무엇을 바꾸는가

기존 결과: BKS가 melt-quench로 망 위상을 만들고, 7net은 그 위에서 relax + NVT MD만 했다.
→ ring 분포가 BKS와 **동일 구조**라 7net의 위상 예측력을 전혀 시험하지 못했다.

S4: **quench를 7net이 수행**한다. Tg를 지나며 위상이 얼어붙는 구간을 7net이 결정하므로,
처음으로 "7net이 만든 망 위상"을 얻는다.

## 고정한 것 / 바꾼 것

| 항목 | 값 | 근거 |
|---|---|---|
| 셀 | 2,160원자, V = 32,652.95 Å³ | 바꾸면 ring 잘림 길이·S(q)의 q_min·g(r) r_max가 달라져 기존 결과와 비교 불가 |
| 밀도 | ρ = 2.20 g/cm³ 고정 | BKS·7net·AIMD·실험이 모두 여기 놓여 있음 |
| 앙상블 | **전 과정 NVT** | Dechant (2026): 용융 중 NPT는 unphysical bond → 비정상 셀 변형. NPT는 300 K에서만 |
| 용융 | BKS 4,000 K 액체 인계 → 7net 재평형 20 ps | classical 액체 + MLIP 재평형은 MLIP 문헌 표준. 성립 조건은 MSD 게이트 하나 |
| 급랭률 | **2×10¹³ K/s** (100 K × 5 ps) | 문헌 범위(10¹¹~10¹³) 상한의 2배. BKS 동일률 통제런으로 상쇄 |
| 300 K 산출 | 평형 5 ps + 생산 5 ps (RDF·궤적) | 7net 본런과 BKS 통제런 모두 동일 길이 |

## 비용 (i5-11600K 6c12t, OMP=6, 실측 1,100 atom-step/s = 1.96 s/step)

| 런 | 길이 | 시간 |
|---|---|---|
| 7net MELT EQUIL 4,000 K | 20 ps | 10.9 h |
| 7net QUENCH 4,000→300 K | 185 ps | 100.7 h |
| 7net EQUIL+PROD 300 K | 10 ps | 5.4 h |
| **7net 합계** | 215 ps | **117 h (4.9일)** |
| BKS 통제런 3종 | — | ~33 min |

스레드는 6이 최적 — 12스레드 무이득, 독립 프로세스도 6개에서 2.0× 포화(메모리 대역폭 한계).
**7net 본런과 BKS 통제런을 동시에 돌리지 말 것.**

## 실행 순서

```bash
cd 02_run/s4_mq7net

# 1) BKS 통제런 먼저 (33분)
bash run_bks_controls.sh

# 2) 7net 본런
ulimit -s 262144
export OMP_NUM_THREADS=6 MKL_NUM_THREADS=6
nohup lmp_7net -in in.mq7net > mq7net.log 2>&1 &
```

`mpirun` 금지 — CPU 빌드에 `e3gnn/parallel`이 없다.

## ★ MSD 게이트 — 시작 후 3시간 지점에 반드시 볼 것

이 계산 전체의 성패가 여기서 갈린다. BKS 액체 구조의 기억이 지워졌는지의 판정이다.

```bash
awk '/^ *Step +Temp/{on=1;next} /^Loop time/{on=0} on&&NF==9{print $1,$2,$7,$8,$9}' \
    mq7net.log | tail -20
#   출력: step  temp  epa  MSD  maxf
#   thermo 9열: step temp press vol density pe v_epa c_msd0[4] c_maxf

# 마지막 한 줄만
awk '/^ *Step +Temp/{on=1;next} /^Loop time/{on=0} on&&NF==9{s=$1;m=$8;f=$9} \
     END{print "step",s," MSD",m,"A^2  maxf",f}' mq7net.log

# 진행 속도 (thermo 100 간격 -> 새 줄 1개 = 100 step)
a=$(grep -cE '^ *[0-9]+ +[0-9]' mq7net.log); sleep 60
b=$(grep -cE '^ *[0-9]+ +[0-9]' mq7net.log)
echo "s/step = $(echo "60/(($b-$a)*100)" | bc -l)"
```

| 판정 | 기준 (step 5000 ≈ 5 ps, 약 3 h 지점) | 조치 |
|---|---|---|
| 통과 | MSD가 시간에 **선형** + 5 ps에 10 Å² 이상 (20 ps에 수십 Å²) | 그대로 방치 |
| 실패 | MSD가 평평하거나 5 ps에 < 5 Å² | 죽이고 `velocity create 4500.0` + `fix nvt 4500`으로 재시작 (손실 3 h) |
| 붕괴 | `c_maxf`가 수십 eV/Å로 튐, epa 발산 | 외삽 실패. 온도를 **낮춰** 3,500 K로 재시도 |

융점은 추정하지 않는다 — 위 로그로 **측정**한다. MLIP 유효 융점이 BKS보다 높은지 낮은지
사전 지식이 없으므로 4,000 K에서 시작해 실패 방향에 따라 올리거나 내린다.

이후엔 하루 1회:

```bash
tail -3 mq7net.log; tail -3 mq7net_profile.dat; cat RESUME.txt
```

## 재개

100 K 구간이 끝날 때마다 `ckpt_T<T>.data`와 `RESUME.txt`가 갱신된다.
죽으면 `RESUME.txt` 내용을 그대로 실행하면 된다.

```bash
cat RESUME.txt
# lmp_7net -in in.mq7net -var domelt 0 -var dfile ckpt_T2600.data -var Tstart 2500
```

`-var domelt 0`이 MELT 단계를 건너뛰고, 체크포인트의 속도를 그대로 이어받는다.
`mq7net_profile.dat`는 append이므로 중복 라인이 생기면 재개 지점 기준으로 손으로 정리할 것.

## 폴더 구조 (2026-08-24 정리)

완주 후 파일이 많아 하위 폴더로 정리했다. 분석 스크립트는 모두 최상위 결과 파일을
직접 참조하므로(기존 프로젝트 관례) `rdf_*`, `prod_*`, `equil*.data`는 최상위 유지.

- `ckpt/`     — 100 K 구간별 체크포인트 38개. 재개용, 런 완주 후엔 참고용
- `logs/`     — `mq7net.log`, `mqbks_*.log`, `bks_controls.log`
- `profiles/` — `mq7net_profile.dat`, `mqbks_*_profile.dat` (`analyze_tg.py`가 여기서 읽음)

## 산출물

| 파일 | 내용 |
|---|---|
| `mq7net_profile.dat` | T · epa · P · MSD — 엔탈피 곡선에서 **7net의 Tg**를 읽는다 |
| `rdf_7net_mq.dat` | 300 K RDF (200 bin, 8 Å) — 기존 `rdf_7net220_3ps.dat`와 동일 포맷 |
| `traj_7net_mq.lammpstrj` | 250 프레임 — ring 분포·S(q)·결합각 분석 입력 |
| `prod_7net_mq.data` | 최종 구조 |
| `mqbks_{5e12,2e13,5e13}_*` | BKS 통제 3종. 같은 이름 규칙 |

## 분석에서 주장할 수 있는 것 / 없는 것

**주장 가능**
- 7net vs BKS @ 2×10¹³ K/s — 초기구조·셀·급랭률·통계 길이가 전부 같다.
  ring 분포 차이가 나오면 **포텐셜에서 온 것**이다. 이 프로젝트에서 처음으로 성립하는 주장.
- BKS 5e12/2e13/5e13 세 점 → **급랭률 민감도**를 %로 제시.
  "7net은 한 속도에서만 돌렸지만, 이 구간의 급랭률 효과는 X %"까지 말할 수 있다.
- `mq7net_profile.dat`의 epa(T) 꺾임 → 7net의 Tg. BKS Tg와 비교.

**주장 불가**
- 기존 7net 220 결과(BKS 위상 위 relax)와 S4 결과의 차이를 "급랭률 때문"이라고 하는 것.
  두 계산은 급랭률 말고도 위상 생성 주체가 다르다.
- AIMD의 한계가 셀 크기라는 것 — 본 계산으로 검증되지 않는다 (기존 문서 규칙 유지).
- 급랭률 2×10¹³이 문헌 범위 안이라는 것 — **상한의 2배다.** 발표에서 명시할 것.

## 알려진 타협

1. 급랭률이 기존 BKS 220 런(5×10¹²)의 4배. CPU 117 h가 한계선이라 불가피.
   → 통제런으로 상쇄하되, 절대값은 발표에서 그대로 밝힌다.
2. 7net은 **시드 1개**만 돌린다 (117 h × N은 불가).
   → ring 분포의 통계 오차는 BKS 다중 시드로 추정해 함께 제시할 것.
3. 300 K 생산 5 ps는 기존 7net 런(3 ps)보다 길지만 BKS 220 런(50 ps)보다 짧다.
   RDF 제1피크 위치에는 충분하나, 비교 표에 길이를 병기할 것.

---

# S4 분석 1 — Ring statistics 해석 (2026-08-24)

그림 `04_analysis/fig/fig_rings_s4.png`, 스크립트 `04_analysis/src/fig_rings_s4.py`,
수치 `04_analysis/dat/S4_rings_summary.dat`, 강건성 `dat/S4_rings_robust.dat`
(`src/ring_robust_s4.py`).

## 무엇을 비교했나 — BKS 4런의 성격을 먼저 정리해야 한다

S4 BKS 통제 3종(5e12 / 2e13 / 5e13)은 **같은 시드(90210)로 같은 4000 K 액체
(`melt4000.data`)를 20 ps 재평형한 동일 지점에서 갈라져 나온다.** 즉 서로 독립 표본이
아니다 — 이 셋의 폭은 **냉각률 효과**이지 통계 산포가 아니다.
(첫 판 관찰문 "BKS끼리는 2.95~3.43 %로 좁게 모여 있다"를 '통계적으로 안정적'이라는 뜻으로
읽으면 안 된다. 세 런이 닮은 건 당연하다.)

그래서 **s0_requench 의 BKS 220 런**(seed 77213, 4000 K **200 ps** 용융, 5e12 K/s)을
네 번째 점으로 넣었다. S4 BKS_5e12 와 **같은 냉각률·다른 실현**이라 BKS 의 실현간 산포를
재는 유일한 자료다. 그리고 이 구조가 바로 **기존 실험에서 7net 이 물려받았던 그 망**이라
(fig_rings.png), 같은 축 위에서 "물려받았을 때 vs 스스로 만들었을 때"가 한눈에 대비된다.

| n | 7net | BKS 평균 | BKS 최소~최대 | 차이(%p) | 오차(%p) | σ |
|---|---|---|---|---|---|---|
| 3 | **7.17** | 2.81 | 1.80 ~ 3.43 | **+4.35** | 1.44 | **3.0** |
| 4 | 15.58 | 14.92 | 14.24 ~ 15.54 | +0.66 | 1.98 | 0.3 |
| 5 | **24.82** | 30.79 | 29.97 ~ 31.61 | **−5.97** | 2.72 | **2.2** |
| 6 | 31.65 | 30.97 | 27.45 ~ 35.69 | +0.68 | 4.27 | 0.2 |
| 7 | 15.67 | 16.45 | 14.83 ~ 18.26 | −0.78 | 2.23 | 0.4 |
| 8 | 4.73 | 3.81 | 3.34 ~ 4.17 | +0.93 | 1.20 | 0.8 |
| 9 | 0.38 | 0.25 | 0.13 ~ 0.33 | +0.13 | 0.29 | 0.4 |

King ring, distinct 기준. 오차 = 계수 Poisson(7net) ⊕ Poisson(BKS 평균) ⊕ BKS 실현간 산포
(5e12 두 실현 차이 / √2, n=2 라 보수적).
평균 고리 크기(distinct): 7net **5.488** vs BKS 5.533 / 5.554 / 5.563 / 5.580.

## 검증 — 먼저 무너질 수 있었던 두 가지를 확인했다

**(1) 결합 cutoff 아티팩트가 아니다.** `ring_stats.py` 는 Si–O 결합을 RCUT = 2.0 Å 고정으로
판정하는데, 7net 구조는 Si–O 가 더 길다(1.635 vs 1.605 Å). 고정 cutoff 가 두 구조에
'같은 기준'이 아닐 수 있고 3-ring 처럼 변형된 작은 고리는 결합 판정 하나에 생겼다 사라진다.
RCUT 을 1.85 ~ 2.15 Å 로 스윕한 결과:

```
rcut     7net  BKS_2e13  BKS_5e12  BKS_5e13  BKS220_s0     (3-ring distinct %)
1.85     7.05      2.96      2.83      3.32       1.80
1.90     7.17      2.95      2.95      3.31       1.80
2.00     7.17      2.95      3.07      3.43       1.80
2.15     7.17      2.95      3.07      3.43       1.80
```

1.90 Å 이상에서 **모든 값이 완전히 평평하고 순위가 한 번도 뒤집히지 않는다.** 아티팩트가 아니다.

**(2) 결함 신호가 아니다.** 7net 이 스스로 만든 망이 결함투성이라면 3-ring 초과는 위상이
아니라 결함 이야기가 된다. 다섯 구조 전부:

| | 다리 O 비율 | 비다리 O (/1440) | 3배위 O | Si 배위≠4 (/720) |
|---|---|---|---|---|
| 7net | 99.583 % | 3 | 3 | 0 |
| BKS 2e13 | 99.722 % | 2 | 2 | 2 |
| BKS 5e12 | 99.792 % | 0 | 3 | 3 |
| BKS 5e13 | 99.653 % | 1 | 4 | 5 |
| BKS220 (s0) | 99.792 % | 1 | 2 | 1 |

원자 1~5개 수준 차이다. **7net 자체 melt-quench 망은 BKS 망만큼 깨끗하다** —
Si 배위≠4 가 0개로 오히려 가장 좋다. RESULTS 7절의 Dechant LES 결함 기준과도 무모순.

**(3) 300 K 미평형 아티팩트도 아니다.** 아래 "P300 부호 반전" 절 참조 — 300 K 10 ps
전 구간에서 압력 드리프트 < 0.02 GPa, MSD 는 소수 3자리까지 고정이다. 300 K 에서
망이 움직일 자유도 자체가 없으므로 ring 분포도 더 변하지 않는다.

## 주장 가능한 것

- **3-ring 이 유일하게 살아남는 차이다.** 7net 7.17 % vs BKS 1.80~3.43 %.
  냉각률을 5e12 → 5e13 으로 10배 바꿔도 BKS 는 2.95~3.43 % 안에서만 움직이고,
  독립 실현까지 넣어도 상한이 3.43 % 다. 7net 은 그 상한의 **2.1배**다.
  실현간 산포와 계수오차를 다 합친 보수적 오차로도 **3.0σ**.
  → *"7net 은 자기 힘으로 망을 만들 때 BKS 보다 3원환(edge-sharing tetrahedra)을
    두 배 이상 많이 남긴다. 이 차이는 냉각률로 설명되지 않는다."*
- **5-ring 은 반대 방향으로 2.2σ** (24.82 vs 30.79). 3-ring 이 늘어난 만큼을 5-ring 이
  내준 형태다. 평균 고리 크기가 7net 5.488 vs BKS 5.533~5.580 으로 작아지는 것과 일관.
- **위상 형성 효과가 실재한다는 것 자체.** 기존 실험에서 7net 은 BKS 망을 물려받아
  ring 분포가 **비트 단위로 동일**했다(RESULTS 5절). 스스로 만들게 하니 3-ring 이
  1.80 % → 7.17 % 로 **4배**가 됐다. 이 프로젝트가 처음 재는 양이고, 크기가 작지 않다.
- **6-ring 으로는 아무 말도 못 한다.** BKS 실현간 폭이 27.45~35.69 %(8 %p)로 가장 넓고
  7net 31.65 % 는 그 한가운데다. 0.2σ. 첫 판 관찰의 "7net 이 6-ring 이 가장 높다"는
  S4 세 런만 볼 때의 착시였다 — 독립 실현 하나를 넣자 사라진다.
  **"7net 망이 더 결정질에 가깝다" 류의 서술은 근거가 없다.**
- 4-ring 도 차이 없다(0.3σ). AIMD 대비 4-ring 부족(RESULTS 5절)은 위상을 스스로
  만들어도 **안 고쳐졌다.**

## 주장 불가 — 반드시 병기할 것

- **7net 은 시드 1개다.** 오차막대의 실현간 성분은 전부 BKS 에서 추정한 것이고,
  7net 이 BKS 와 같은 크기의 실현간 산포를 갖는다는 보장이 없다. 3.0σ 는
  **"BKS 의 산포를 7net 에도 적용했을 때"** 의 값이다. 117 h/런이라 반복이 불가능했다.
  → 발표 문장은 *"유의하다"* 가 아니라 *"BKS 의 실현간 산포를 기준으로 재면 3σ 수준이고,
    7net 자체 반복이 없어 이 이상은 말할 수 없다"* 로 쓴다.
- **3-ring 초과가 실물 유리에 대해 옳은가/틀린가는 이 계산이 답하지 못한다.**
  ring 분포는 회절로 재는 양이 아니라 모델에서 유도하는 양이라 **실험 계열이 없다**
  (RESULTS 5절 경고와 동일). AIMD 참조(Dechant)는 120원자·5 ps quench 라
  3-ring 통계를 신뢰할 표본이 아니다.
- **`fig_rings.png` 와 같은 그림에 섞지 말 것.** 그건 "물려받은 위상" 실험이고
  이건 "스스로 만든 위상" 실험이다. 대비시키려면 나란히 놓되 축을 명시할 것.
  (fig_rings_s4.png 패널 (c) 는 s0 점에 화살표로 그 대비를 표시해 두었다.)

## 다음에 이 결론을 강화하는 가장 싼 방법

`run_bks_sweep.sh` 의 **2e13_r2 / 2e13_r3 반복**이다 — 매칭 냉각률에서 BKS 실현 3개가
생기면 위 표의 '실현간 산포' 항이 추정치가 아니라 실측치가 된다.
그런데 `in.mqbks_fast` 가 profile 만 뽑고 구조를 안 남겨 ring 분석이 불가능했다.
**2026-08-24 에 300 K 평형 5 ps + `write_data prod_bks_${tag}.data` 를 추가했다**
(런당 +9초, 궤적·RDF 는 여전히 안 만듦). 스윕이 아직 미실행이라 손실 없이 반영됐다.

---

# S4 분석 2 — P300 부호 반전은 미평형이 아니다 (2026-08-24)

`NEXT_SESSION_PROMPT.md` 의 미결 항목 3. **추가 계산 없이 기존 로그로 답이 나왔다.**

`logs/*.log` 의 300 K 구간(평형 5 ps + 생산 5 ps)을 전·후반으로 갈라 압력 평균을 보면:

| 런 | 평형 전반 | 평형 후반 | 생산 전반 | 생산 후반 | 요동 sd |
|---|---|---|---|---|---|
| **7net** | **+0.781** | **+0.774** | **+0.797** | **+0.795** | 0.117 |
| BKS 2e13 | −3.118 | −3.054 | −3.025 | −3.041 | 0.121~0.206 |
| BKS 5e12 | −2.797 | −2.745 | −2.755 | −2.844 | 0.079~0.123 |
| BKS 5e13 | −3.245 | −3.154 | −3.262 | −3.261 | 0.067~0.133 |

(GPa. 프로파일의 P300 은 quench 마지막 100 K 구간 평균이고, 위는 그 **다음** 10 ps 다.)

- 10 ps 전 구간에서 **드리프트가 요동 sd 의 1/5 미만**이다. 단조 추세도 없다.
- 같은 구간 MSD 는 7net 150.9 Å² 에서 **소수 3자리까지 고정** — 300 K 에서 확산이 0이다.

→ **+0.80 GPa 는 '아직 안 풀린 응력'이 아니라 얼어붙은 망 위상 자체의 성질이다.**
300 K NVT 를 더 길게 끌어도 나올 것이 없다. 움직일 자유도가 없다.

## 그래서 검증 설계를 바꿨다 — NPT 대신 0 K E-V 스캔

남은 진짜 질문은 "이 망이 원하는 밀도가 얼마냐"이고 그건 **부피 자유도**를 풀어야 답이 난다.

| 경로 | 비용 | 판단 |
|---|---|---|
| 300 K NPT 100 ps (7net) | 100,000 step × 1.96 s = **54 시간** | 불가. 20 ps 로 줄여도 11 h 이고 barostat 이 자리잡기에 짧다 |
| **0 K E-V 스캔 9점** | 9 × ≤150 eval × 1.99 s ≈ **45분** | 채택 |

E-V 를 택하는 적극적 근거: **RESULTS 2절의 ρ₀ = 2.2185 (+0.84 %) / K₀ = 43.2 GPa 가
바로 이 방법으로 나온 값이다.** 같은 자로 재야 "위상 형성 효과"가 밀도에서 얼마인지
직접 뺄셈이 된다. NPT 로 재면 방법이 달라 그 뺄셈이 성립하지 않는다.

- 스크립트: `in.ev_s4`, `run_ev_s4.sh` (2026-08-24 작성, **dhl-desktop 에서 실행**)
- 창을 f = 0.94~**1.10** 으로 넓혔다(기존 in.ev220 은 0.94~1.06). P300 = +0.80 GPa 와
  K ≈ 43 GPa 로 어림하면 ρ₀ ≈ 2.16 이라 기존 창의 **하한(2.157)에 평형점이 걸린다.**
  RESULTS 2절이 "끝점이 곡률의 곡률을 결정한다"고 경고한 그 상황이다.
- 예상되는 결론(**아직 미측정 — 예상일 뿐이다**): 자체 형성 망의 ρ₀ 가 2.16 근처면
  "7net 이 밀도 오차를 7.8배 줄였다"는 헤드라인이 **위상까지 스스로 만들면 일부
  되돌아간다**는 뜻이 된다. 방향으로는 3-ring 초과(더 성긴/변형된 망)와 일관된다.
- BKS 대조는 `in.ev_bks` 를 `prod_bks_2e13.data` 로 돌리면 수 분이면 된다. 짝을 맞춰라.

---

# S4 분석 3 — 확장 스윕 반영 후 Tg (2026-08-24, 2차)

`run_bks_sweep.sh` 완주. BKS 프로파일 10개(냉각률 8종 + 2e13 반복 3개).
`fig_tg_s4.png` / `tg_s4_summary.dat` 재생성. **얻은 것 하나, 잃은 것 하나.**

## 얻은 것 — Tg 의 시드간 오차막대 (이 프로젝트 최초)

매칭 냉각률 2×10¹³ K/s 반복 3개: **Tg_arrest = 2800 / 3000 / 2500 K, sd = 252 K.**

지금까지 "같은 냉각률에서 7net 이 BKS 보다 800 K 낮다"를 오차막대 없이 써 왔다.
이제 정확히는 **767 K, BKS 시드 산포 sd 252 K 기준 3.0σ** 다. 결론은 살아남지만
**"800 K 낮다"를 단독 수치로 인용하면 안 된다** — 100 K 격자 판정 + 250 K 시드 산포다.

7net 은 여전히 시드 1개다(117 h/런). 3.0σ 의 산포 성분은 전부 BKS 에서 온 것이고,
7net 이 같은 크기의 산포를 갖는다는 보장은 없다. ring 통계와 **같은 형태의 한계**다.

### 더 쓸모 있는 환산 — 냉각률 몇 decade 어치인가

유효 8개 냉각률로 회귀하면 **Tg = 177 ± 44 K/decade × log₁₀(rate)**.
(*decade* = 로그축 용어로 **10배(한 자릿수)**. 연대(年代)가 아니다.
 즉 급랭을 10배 빠르게 할 때마다 Tg 가 177 K 올라간다.)
7net-BKS 간극 767 K 를 이 기울기로 나누면 **4.3 decade** (기울기 ±1SE 로 3.5 ~ 5.8).

→ *"BKS 를 7net 만큼 낮은 Tg 로 끌어내리려면 급랭을 10⁴배 느리게 해야 한다."*
   스윕이 실제로 훑은 범위(10¹²~10¹⁴, 2 decade)의 **두 배 밖**이다.
   "포텐셜 차이가 냉각률로는 흉내낼 수 없는 크기"라는 주장이 이제 수치로 선다.
   ★ 단, 이 환산은 회귀 기울기가 스윕 구간 밖까지 선형이라고 **외삽**한 것이다.
     실제 Tg(rate)는 느린 쪽에서 완만해지므로 4.3 decade 는 **하한**에 가깝다.

## 잃은 것 — 2×10¹⁴ K/s 점은 못 쓴다 (그리고 왜 미리 몰랐나)

ΔMSD/100 K 는 구간 **시간**에 비례한다. 구간 길이가 1e12 에서 100 ps, 2e14 에서 0.5 ps —
**200배** 차이다. 원래 4개 런(5e12~5e13)은 10배 차이뿐이라 이 문제가 안 보였다.

| rate | 구간 | 액체(T≥3700K) ΔMSD | 임계값 대비 | 판정 |
|---|---|---|---|---|
| 1e12 | 100 ps | 18.65 | 186× | 정상 |
| 2e13 | 5 ps | 1.03 | 10× | 정상 |
| 5e13 | 2 ps | 0.39 | 3.9× | 통과, 여유 적음 |
| 1e14 | 1 ps | 0.27 | 2.7× | 통과, **경계선** |
| **2e14** | **0.5 ps** | **0.072** | **0.7×** | **불가** |

2e14 는 **액체 상태의 ΔMSD 가 이미 임계값 아래**다. 그러니 급랭 시작 직후 판정이 걸리고,
실제로 arrest = 3800 K (출발 4000 K 에서 200 K)가 나왔다. caloric 피팅은 액체 가지 점이
없어 실패했다 — **코드가 스스로 "이 값 못 쓴다"고 말한 것**이고, 그 신호를 게이트로 만들었다.

→ `fig_tg_s4.py` 에 **동적 범위 게이트** 추가: 액체 ΔMSD < 2.5 × 임계값이면 제외.
   현재 2e14 하나만 걸린다. **임계값 0.10 과 K=5 는 건드리지 않았다** (재도출 금지 항목).
   이 게이트는 임계값을 바꾸는 게 아니라 *임계값이 적용 가능한 구간인지*를 판정한다.

⚠ **1e14 는 2.7× 로 경계선이다.** 회귀에 넣긴 했지만, 이 점을 빼고 다시 피팅해
기울기가 크게 움직이면 위 4.3 decade 환산을 다시 봐야 한다. (아직 안 해봤다.)

⚠ **더 빠른 냉각률을 재려면 프로파일 구간 길이를 늘려야 한다** — 100 K 구간을 잘게
   쪼개 ΔMSD 를 더 자주 적분하거나, 구간당 ave/time 창을 고정 물리시간으로 잡아야 한다.
   지금 설계로는 2e14 이상이 원리적으로 측정 불가다. 이건 설계 한계지 버그가 아니다.

---

# S4 분석 4 — E-V 스캔을 2단계로 재설계 (2026-08-24, 2차)

사용자 지적: **"E-V 는 보통 equilibrium 에 대칭으로 점을 찍지 않나?"** 맞다.

1차 설계(f = 0.94~1.10 고정 격자)는 *추정* 평형 f₀ ≈ 1.0185 기준으로는 −7.7 %/+8.0 %
라 우연히 대칭에 가까웠다. 그러나 **그 중심이 추정치다.** P300/K 선형 외삽으로 잡은
값인데 실리카는 K₀′ < 0 이라 선형 외삽이 계통적으로 치우친다. 대칭인 척만 하는 격자다.

## 창 비대칭이 실제로 얼마나 문제인지부터 쟀다

기존 스캔들을 BM3 P(V) 로 다시 피팅해 확인 (구현 검증도 겸함 — 전 7점 결과가
RESULTS 2절 보고값과 일치):

| | n | ρ₀ | K₀ | K₀′ | V/V₀ 범위 |
|---|---|---|---|---|---|
| 7net 전 7점 (보고값) | 7 | 2.2185 | 43.23 | −2.02 | 0.948~1.069 (3압축/4팽창) |
| 7net V₀ 대칭 부분집합 | 6 | 2.2186 | 43.17 | −1.93 | 0.948~1.049 (3/3) |
| | | **+0.004 %** | **−0.2 %** | | |
| BKS 전 7점 (보고값) | 7 | 2.3442 | 34.33 | −6.87 | 0.959~1.087 (2압축/5팽창) |
| BKS V₀ 대칭 부분집합 | 4 | 2.3465 | 33.16 | −3.95 | 0.960~1.024 (2/2) |
| | | +0.100 % | **−3.4 %** | | |

→ **7net 쪽은 창 비대칭이 거의 영향 없다(K₀ 0.2 %).** BKS 가 3.4 % 흔들리는 건 창이
아니라 절단 꼬리 artifact 다(RESULTS 2절: BKS 잔차가 `− + + + − − +` 로 구조를 갖는다).
즉 **이번에 고쳐야 할 것은 창 폭이 아니라 중심**이다. 창 폭은 기존과 같은 자릿수
(±6 %)로 맞춰 두면 "위상 형성 효과"에 창 효과가 섞이지 않는다.

## 새 설계 — 1단계로 V₀ 를 실측하고, 2단계 격자를 그 중심으로 생성

- **1단계 (pilot, 4점, ~20분)**: f = 0.98 / 1.00 / 1.02 / 1.04.
  P(f) 2차 최소제곱으로 P=0 근을 찾아 f₀·ρ₀·국소 K 를 얻는다.
  ★ 점 하나(5분) 더 써서 **양쪽에서 괄호친다** — 한쪽에만 찍으면 2차 피팅이 외삽이 되고
  그 위에 2단계 격자 7점(35분)을 세우므로 빗나갈 때 손실이 훨씬 크다.
  근이 구간 밖이면 **격자를 만들지 않고 메시지와 함께 중단**한다(외삽 방지).
- **2단계 (main, 7점, ~35분)**: V₀ 기준 **V/V₀ = 0.94~1.06 등간격** — 정의상 대칭이고
  V₀ 자신이 격자 중앙에 들어간다(홀수 점).

### 예상 위치를 실측으로 고정했다 (열압력 보정)

P(300 K) = +8,000 bar 를 그대로 쓰면 안 된다 — 열압력이 섞여 있다.
같은 포텐셜·같은 밀도에서 **기존 구조로 실측**했다 (`s3_md/nvt_7net220.log`):

| | 값 |
|---|---|
| 7net 300 K NVT @ρ=2.20 | −2,950 bar |
| **같은 구조** 0 K virial (`ev220_scan.txt` f=1.00) | −3,654 bar |
| **열압력** | **+704 bar** (운동항 +2,740, 열 virial −2,036) |

→ S4 자체 형성 망의 0 K P ≈ +7,300 bar = **+0.73 GPa**, K ≈ 43 GPa 이므로
   ΔV/V ≈ 0.017 → **f₀ ≈ 1.017, ρ₀ ≈ 2.163 g/cc**. pilot 구간(0.98~1.04) 한가운데다.

합성 데이터 복원 시험(ρ₀=2.163·K=43·K′=−2 를 심고 4점만 줌):
**f₀ 1.01714 (참값 1.01711), ρ₀ 2.1629 (2.1630), K 42.84 (43.00)**. 충분하다.
근이 구간 밖인 경우도 시험 — 격자를 만들지 않고 exit 1 로 멈춘다.

### numpy 의존 제거 (선택이지 필수는 아니었다)

pilot 파서와 격자 생성을 numpy 없이 다시 썼다. 2차 최소제곱은 3×3 정규방정식이라
표준 라이브러리(Cramer)로 충분하다. 현재 외부 의존은 `lmp_7net`, `python3`(stdlib),
`flock` 뿐이다.

> ⚠ **정정(2026-08-24).** 처음에 이걸 "안 고쳤으면 실행이 실패했을 요인"으로 적었는데
> **틀렸다.** dhl-desktop 에는 numpy 가 있다(사용자 확인). `lmp_7net` 을 `mlip` 환경에서
> 돌리므로 그 python3 에 numpy 가 딸려 온다. 근거는 이 NOTE 의 "Mac 샌드박스엔 conda 가
> 없다"를 dhl-desktop 까지 **확인 없이 일반화**한 것이었다.
> stdlib 판을 유지하는 이유는 "실패를 막으려고"가 아니라 **의존을 하나 줄이는 게
> 손해가 아니라서**다. 이 구분을 흐리지 말 것 — 두 머신의 환경을 섞어 추측하는 것이
> 이 프로젝트에서 반복되는 실수다(아키텍처 절 참조).

- `run_ev_s4.sh` 가 두 단계를 자동으로 잇는다(인자: dfile, tag, 반폭, 점개수).
- 출력: `ev/ev_s4_<tag>_pilot.txt`, `ev/ev_s4_<tag>_scan.txt`
- BM3 피팅은 scipy 가 필요해 **Mac 샌드박스에서 못 돌린다** — dhl-desktop 의 `mlip` 환경.

---

# S4 분석 5 — (a) 패널이 실제로 말하는 것: 배열 에너지 (2026-08-24, 3차)

사용자 질문: "(a)는 어떤 의미를 가진다고 봐야 하나?" — 재보니 **Tg 그림에서 가장
독립적인 정보가 여기 있었는데 그림이 그걸 안 보여주고 있었다.** 고쳤다.

## 잰 것

| | 유리가지 dE/dT<br>(T≤1500 K) | 액체가지 dE/dT<br>(T≥3200 K) | 비 | E(4000)−E(300) | **배열 성분** |
|---|---|---|---|---|---|
| **7net** | **0.1341** | **0.3141** | **2.34** | 745.6 | **249** |
| BKS (9런) | 0.1327~0.1345 | 0.165~0.228 | 1.25~1.71 | 546~602 | 71~106 |

단위 meV/atom/K, meV/atom. 조화 고전 극한(위치에너지분) = 1.5 k_B = **0.1293**.

## 읽는 법 — 세 가지가 한 패널에 있다

**(1) 검증.** 유리가지 기울기가 **11개 런 전부 1.3 % 안에서 일치**하고 1.5 k_B 의
1.04배다. Tg 아래에서는 두 포텐셜이 **구별되지 않는다** — 순수 조화 진동만 남는다.
RESULTS 7절의 "E(300 K) − E(0 K) = 39 meV/atom = 3k_BT/2" 와 같은 이야기를
전 온도구간에서 다시 확인하는 셈이다. 이게 맞지 않으면 나머지를 볼 필요가 없다.

**(2) caloric Tg.** 두 직선의 교점. 2차 지표(수치는 `tg_s4_summary.dat`).

**(3) ★ 진짜 내용 — 배열(configurational) 에너지.** 유리가지 기울기를 4000 K 까지
외삽한 선(그림의 회색 기준선)이 "진동만 방출됐다면" 곡선이다. 실제 곡선이 그 위로
벌어진 만큼이 **망이 재배열하며 내놓은 에너지**다:

> **7net 249 meV/atom vs BKS 71~106 — 2.4~3.5배.**

7net 의 액체는 **배열 자유도에 훨씬 많은 에너지를 담고 있는 다른 액체**다.
액체가지 열용량으로 봐도 7net 0.314 vs BKS 0.165~0.228 (약 1.5배).

★ **절대 epa 는 포텐셜 간 비교 불가지만 기울기는 비교 가능하다** — 미분이라 에너지
  기준점이 상쇄된다. RESULTS 의 "epa 절대값 비교 금지" 규칙과 모순되지 않는다.
  이 점을 명시하지 않으면 규칙 위반으로 오해받는다.

## 세 결과가 한 방향을 가리킨다

- 배열 에너지 2.4~3.5배 (분석 5) — 7net 액체가 배열 자유도를 훨씬 많이 쓴다
- Tg 767 K 낮음 (분석 3) — 배열 저장고가 큰 액체는 더 오래 유동적이다
- 3-ring 2.1배 (분석 1) — 그렇게 얼어붙은 망은 더 변형돼 있다

**독립적으로 잰 세 양이지만 서로 모순되지 않는다.** 다만 이건 **정합성**이지 인과가
아니다. "배열 에너지가 커서 Tg 가 낮고 그래서 3-ring 이 많다"는 **가설**이고,
본 계산으로 인과 사슬을 분리 검증하지는 않았다. 발표에서는 "세 지표가 같은 방향"까지만.

⚠ BKS 액체가지 기울기가 0.165~0.228 로 ±16 % 흩어진다. 5 ps 평균이라 통계가 짧다.
   BKS 범위를 좁은 값으로 인용하지 말 것.

---

# S4 분석 6 — (c) 표시 최종안 (2026-08-24, 4차)

여러 번 뒤집었다. **최종 상태와 그 이유만 남긴다.**

## 점: 반복 냉각률은 **중앙값**

2e13 세 런 = 2800 / 3000 / 2500 K. 중앙값 **2800**, 평균 2767.
- arrest 판정이 **100 K 격자 위의 계단함수**라 평균 2766.7 은 이 측정이 원리적으로
  낼 수 없는 값이다. 중앙값은 실측된 격자 위 값이다.
- 두 값 차이 33 K = 격자 간격의 1/3. **어느 쪽을 써도 결론은 안 바뀐다.**
- 실제로 중앙값 쪽 회귀가 잔차 sd 72 K 로 평균(79 K)보다 약간 낫다.
- ★ 부수 효과: 간극이 **정확히 800 K** 가 되고 환산이 **10^4.4배**가 된다.
  (평균이면 767 K / 10^4.3배. 이 정도 흔들림이 이 수치의 정밀도다 — 소수 첫째자리를
   유효숫자처럼 인용하지 말 것.)

## 오차막대: **안 그린다** (`SHOW_SEED_SPREAD = False`)

8개 점 중 하나만 막대가 서면 **그 점이 무슨 특별한 것처럼** 읽힌다. 실제로는
"같은 조건을 세 번 돌려 중앙값을 쓴" 것뿐이다.
시드 산포(2500~3000 K, sd 252 K)는 **결론 중 하나가 맞지만 그림이 들 짐은 아니다** —
`tg_s4_summary.dat` 가 세 런을 다 들고 있고 NOTE 분석 3 이 해석을 들고 있다.
플래그를 켜면 n=3 지점에 sd 가 아닌 **실측 최소~최대**가 그려진다
(n=3 은 정규분포를 가정할 표본이 아니므로 sd 를 쓰지 말 것).

## 범례: 수치를 다 뺐다

`BKS linear fit` / `BKS controls` / `7net-Nano-4.5` 세 줄뿐이다.
- **기울기 180±40 을 범례에서 뺐다.** 이 패널의 논점은 "7net 이 추세 아래에 있다"이지
  기울기의 정밀도가 아니다. 수치는 stdout·`tg_s4_summary.dat`·NOTE 에 있다.
- **"1 seed" 표기도 뺐다.** 7net 이 시드 1개라는 건 그림이 아니라 **문장으로 병기할
  한계**다(발표·문서에서 반드시 말할 것). 범례에 넣으면 다른 점들은 여러 시드인 것처럼
  읽히는데 사실 BKS도 2e13 말고는 전부 1개다 — 오히려 오해를 만든다.

## 신뢰밴드: **뺐다**

밴드가 하던 일(추세의 불확도)은 범례의 `180 ± 40 K per ×10 rate` 와 800 K 화살표가
이미 한다. 색면을 하나 더 얹을 값어치가 없다.

⚠ **되살릴 거면 형태를 반드시 고쳐서 살려라.** 1차 판은 **기울기만 ±1 SE 로 흔들고
절편을 고정**해 그렸다. 그러면 밴드가 x = 0 (= log₁₀rate = 0, 곧 rate 1 K/s — 그림
한참 밖)을 축으로 회전해 **데이터 구간 전체에서 부풀고 오른쪽으로 갈수록 더 벌어진다.**
실제 불확도 모양이 아니다. 올바른 형태는 중심에서 가장 좁은 모래시계다:

```
SE(x) = s_res * sqrt( 1/n + (x - x̄)² / Σ(x - x̄)² )     s_res = sqrt(SSR/(n-2))
```

이건 **최소제곱의 닫힌 해**다 — 초기값을 흔들어 수렴을 보는 식의 수치실험이 아니다.
`np.polyfit` 은 정규방정식/SVD 로 한 번에 푼다. 반복도 초기값도 없다.
올바른 형태로 계산하면 잔차 sd 72 K, 중심 최소폭 ±27 K 로 **1차 판보다 훨씬 좁다** —
즉 그 버그는 결론을 **약해 보이게** 만들고 있었다.

---

# S4 분석 7 — 그림 표시 규칙 (2026-08-24, 4차)

사용자 지시로 되돌리거나 뺀 것들. **이유를 남긴다 — 다시 넣지 말라는 뜻이다.**

- **BKS 곡선 투명도 제거(복원).** 반투명이면 7net 이 도드라지지만 **냉각률
  그라데이션(옅을수록 느림)이 안 보인다.** (a) 가 갖고 있던 정보 하나가 통째로 사라지는
  거래라 손해다. 불투명 유지.
- **7net zorder 승격 제거.** 불필요 — BKS 선이 더 얇아 저온 겹침 구간에서도 비치고,
  고온에서는 어차피 갈라져 나온다.
- **arrest 세로선: alpha 0.85, `zorder=0`(맨 뒤).** 흐리면 안 보이고, 앞에 있으면
  데이터 선을 가린다. 진하게 하되 뒤로 보낸다.
- **반복런(_r2/_r3)의 dotted 선종 제거 → 전부 dashed.** (b) 에서 선 종류가 두 가지로
  보이면 "다른 무엇"처럼 읽힌다. 같은 냉각률의 같은 계산이므로 구분할 이유가 없다.
- **배열 에너지 화살표·숫자 삭제.** 이 양은 **T = 4000 K 에서 정의된다**(그 온도에서의
  곡선-기준선 간격). 그런데 4000 K 는 축 오른쪽 끝이라 자리가 없어 1차 판은 T = 3560 /
  3930 에 세웠고 — **거기서는 실제 간격과 맞지 않았다.** ⚠ 눈으로 잡힌 버그다.
  다시 넣으려면 T = 4000 K 에 세우거나 x 축을 4200 K 까지 늘려 여백을 만들 것.
  수치(7net 249 vs BKS 71~106)는 NOTE 분석 5 에만 둔다.
- **"configurational energy released" 모서리 텍스트 삭제.** 화살표가 없으니 무의미.
- **기준선 라벨은 "harmonic baseline" 만, 선과 평행하게.** 회전각은 화면좌표에서 재야
  하므로 `fig.tight_layout()` **뒤에** 계산한다 (먼저 하면 축 상자가 바뀌어 각이 틀어진다).
- **컬러바 안 단다.** (c) 가 이미 색↔냉각률을 점으로 읽게 해주므로 컬러바는 같은 정보를
  축 하나 더 써서 반복하는 셈이다. 방향만 (a) 범례에 한 줄
  ("light = slow, dark = fast")로 알린다.
- **제목·축라벨 첫 글자는 대문자.** `fig_rings_s4.py` 도 같은 규칙으로 맞췄다.

---

# S4 분석 8 — E-V 결과: RESULTS 2절의 예측이 검증됐다 (2026-08-24)

스캔 `ev/ev_s4_7net_scan.txt` (2단계, V₀ 대칭 7점). 피팅 `04_analysis/src/bm3.py`,
요약 `04_analysis/src/ev_s4_summary.py` → `04_analysis/dat/ev_s4_summary.dat`.

## 먼저 품질 확인 (통과)

- **pilot 이 V₀ 를 5 bar 안으로 맞췄다.** 격자 중앙점(f=1.00623, ρ=2.1594)의
  P = **+4.74 bar**. 열압력 보정(+704 bar)까지 넣은 예측이 실측과 맞았다는 뜻이다.
- **E(V)/P(V) 두 독립 경로 일치**: ρ₀ **0.006 %**, K₀ **0.7 %**.
  기존 스캔(0.008 % / 0.4 %)과 같은 수준 → 응력 구현·피팅 절차 정합.
- P 잔차 RMS 99 bar (기존 스캔 44 bar). 2.2배 크지만 스캔 폭 ±23,000 bar 의 0.4 %.
  부호패턴 `+ - - + + + -` 로 구조 없음(무작위) → BKS 의 절단꼬리 artifact 같은
  계통오차는 없다.
- ⚠ `minimize` 가 7점 모두 150 eval 상한에서 멈춘다(maxf 0.013~0.105 eV/Å).
  **기존 ev220 스캔도 동일 설정·동일 거동**(maxf 0.005~0.114)이므로 비교는 공정하다.
  절대값을 논문에 쓰려면 상한을 올려 재확인할 것.

## 결과 — 밀도는 나빠지고 탄성률은 좋아진다

| | ρ₀ | 실험 대비 | K₀ | K @ρ=2.20 | 실험(37) 대비 |
|---|---|---|---|---|---|
| BKS 망 위 BKS | 2.3442 | +6.55 % | 34.33 | 45.33 | +22.5 % |
| BKS 망 위 7net (기존 보고값) | 2.2185 | +0.84 % | 43.23 | 43.92 | +18.7 % |
| **7net 자기 망 위 7net (S4)** | **2.1595** | **−1.84 %** | **39.28** | **38.11** | **+3.0 %** |

★ K 는 **반드시 같은 밀도에서** 비교한다(RESULTS 2절의 함정). 위 `K @ρ=2.20` 열이 그것이다.

## ★ 이게 왜 큰 결과인가 — RESULTS 2절이 한 예측의 직접 검증

RESULTS 2절은 이렇게 썼다:

> "실험 밀도에서 둘 다 약 20 % 딱딱하다 … **K₀ 오차의 출처는 포텐셜이 아니라 구조 생성
> 프로토콜이다. 포텐셜을 MLIP 로 갈아끼워도 이 오차는 안 줄어든다. 거시 물성을 맞추려면
> MLIP 로 melt-quench 를 직접 하고 현실적인 냉각속도까지 내려가야 한다.**"

**S4 가 그 예측의 앞부분을 실행했고, 예측대로 됐다.**
포텐셜을 그대로 둔 채 **망만 7net 이 만든 것으로 바꾸자** ρ=2.20 에서

> **K = 43.92 → 38.11 GPa. 실험(37)까지의 간극 6.92 GPa 중 84 %가 사라졌다.**

같은 밀도에서 비교했으므로 이건 순수 **위상 효과**다. 포텐셜도, 셀도, 밀도도 같다.
이 프로젝트가 "구조 생성 프로토콜이 범인"이라고 진단했던 것을 **직접 실험으로 확인**한 셈이다.

## 그런데 밀도는 반대로 나빠졌다 — 이걸 숨기지 말 것

ρ₀ 오차가 **+0.84 % → −1.84 %** (절대값 2.2배). 부호도 뒤집혔다(과대 → 과소).
"MLIP 가 밀도 오차를 7.8배 줄였다"는 헤드라인은 **BKS 위상을 물려받았을 때의 이야기**다.
자기 힘으로 망을 만들면 그 이점의 상당 부분이 사라진다. BKS(+6.55 %) 대비로는
여전히 3.6배 개선이지만 7.8배는 아니다.

**한 지표는 좋아지고 한 지표는 나빠졌다.** 이걸 "MLIP 승리"로 뭉뚱그리면 안 된다.

## 예측과 다른 부분 — 냉각률을 안 낮췄는데도 K 가 맞았다

RESULTS 2절은 **"MLIP melt-quench **그리고** 현실적 냉각속도"** 둘 다 필요하다고 했다.
S4 는 앞의 것만 했고 냉각률은 오히려 2×10¹³ K/s(기존 BKS 런의 4배 빠름)인데도
K 오차가 +18.7 % → +3.0 % 로 떨어졌다.

→ **위상 생성 주체의 효과가 냉각률 효과보다 훨씬 크다**는 뜻일 수 있다.
   단 **이 계산으로 분리 검증하지 않았다.** 대안 해석이 남아 있다:
   (i) 급랭이 만든 성긴 망(ρ₀ 낮음)이 우연히 무른 쪽으로 밀어 실험값과 만났다
       — 즉 RESULTS 가 BKS 에서 지적한 **"두 번 틀려서 한 번 맞음"의 재현**일 수 있다.
       실제로 밀도가 나빠진 방향과 K 가 좋아진 방향이 실리카의 K′<0 에서는 같은 방향이다.
   (ii) 진짜로 위상이 지배적이다.
   **(i)/(ii) 를 가르려면 냉각률을 바꾼 7net 런이 필요하다 — 117 h/런이라 현재 불가.**
   발표에서는 **"같은 밀도에서 비교해도 K 가 좋아졌다"까지만** 주장하고
   (같은 밀도 비교가 (i) 을 상당 부분 배제한다), 인과는 열어둘 것.

## 네 지표가 같은 방향

3-ring 2.1배 ↑ · 배열 에너지 2.4~3.5배 ↑ · Tg 800 K ↓ · **K 13 % ↓(같은 밀도)**
→ 7net 이 스스로 만든 망은 **더 변형되고 더 무른 망**이다. 네 지표가 모순 없이 그 그림을 준다.
다시 말하지만 **정합성이지 인과 사슬의 분리 검증이 아니다.**

## 남은 것

- **BKS 짝 미실행**: `in.ev_bks` 를 `prod_bks_2e13.data` 로. 수 분.
  현재 비교의 BKS 항은 **s0 의 5e12 K/s 망**이라 냉각률이 다르다.
  매칭 냉각률(2e13) BKS 망으로도 같은 표를 만들어야 "위상 형성 효과"에서
  냉각률 성분이 완전히 빠진다.
- 시드 1개. ring·Tg 와 같은 한계.

## ★ 냉각률 성분 분리 — 완료 (2026-08-25)

②(7net 이 BKS 2e13 망을 읽음)와 ④(BKS 가 BKS 2e13 망을 읽음)까지 다 돌았다.
표 전체는 `04_analysis/dat/ev_s4_summary.dat`. **읽는 포텐셜을 7net 으로 고정**하고
두 축을 분리한 결과:

| | ρ₀ | K @ρ=2.20 | 실험(37) 대비 |
|---|---|---|---|
| ① 7net / BKS 망 5e12 (기존 보고값) | 2.2185 (+0.84 %) | 43.92 | +18.7 % |
| ② 7net / BKS 망 **2e13** | 2.2551 (+2.51 %) | 42.98 | +16.2 % |
| ③ 7net / **7net 자기 망** 2e13 | 2.1595 (−1.84 %) | 38.11 | **+3.0 %** |

**[A] 냉각률 효과 (① → ②)** : K@2.20 **−0.94 GPa (−2.1 %)**
**[B] 위상 형성 효과 (② → ③)** : K@2.20 **−4.87 GPa (−11.3 %)**, 실험 간극의 **81 %** 제거

> **총 −5.81 GPa 중 냉각률이 −0.94(16 %), 위상 생성자가 −4.87(84 %).
> 위상 효과가 냉각률 효과의 5.2배다.**

냉각률 4배를 바꿔도 K 는 2 %밖에 안 움직이는데, **같은 냉각률에서 망 만드는 주체만
바꾸면 11 % 움직인다.** RESULTS 2절의 "K₀ 오차의 출처는 포텐셜이 아니라 구조 생성
프로토콜" 이라는 진단이 이제 **수치로 분해된 형태로** 확인됐다.

### 창(부피구간) 효과가 아님을 증명했다

각 행을 전 점 / 공통창(|ln(V/V₀)| ≤ 5 %) 두 번 피팅해 K@2.20 을 대조했다:

| 행 | 전 점 | 공통창 | 차이 |
|---|---|---|---|
| 7net / BKS 5e12 | 43.92 | 43.80 | **−0.3 %** |
| 7net / BKS 2e13 | 42.98 | 43.11 | **+0.3 %** |
| 7net / OWN 2e13 | 38.11 | 37.76 | **−0.9 %** |
| *BKS / BKS 5e12* | *45.33* | *41.89* | *−7.6 %* |
| *BKS / BKS 2e13* | *45.95* | *36.77* | *−20.0 %* |

[A] 는 −0.94 → −0.70, [B] 는 −4.87 → −5.35 로 **창을 바꿔도 결론이 유지된다.**

⚠ **반면 BKS 행은 창에 따라 K 가 최대 20 % 움직인다** (잔차도 292 / 793 bar).
   RESULTS 2절의 절단꼬리 artifact 가 넓은 창에서 크게 드러난 것이다.
   → **BKS 행의 K 는 인용하지 말 것.** [A]·[B] 는 7net 행끼리의 뺄셈이라 무관하다.

### 밀도는 두 효과가 반대로 작동한다

- **[A] 냉각률**: 2.2185 → 2.2551 (**+1.65 %**) — 빠른 급랭인데 오히려 **더 조밀**해졌다.
  통상 기대(빠를수록 성김)와 반대다. BKS reader 로 봐도 같은 방향(+1.86 %)이므로
  구조의 실제 성질이지 읽는 포텐셜의 문제가 아니다.
  ⚠ 단 두 망은 냉각률 외에 시드(77213/90210)·용융이력(200 ps / 20 ps 재평형)·
    300 K 평형(100 ps / 10 ps)도 다르다. **[A] 를 "순수 냉각률 효과"로 부르지 말 것** —
    "BKS 망 두 실현의 차이"가 정확한 표현이고, 그 차이가 K 에서 2 % 라는 게 요점이다.
- **[B] 위상**: 2.2551 → 2.1595 (**−4.24 %**)
- 순효과: +0.84 % → −1.84 %. 밀도 오차는 **절대값이 2.2배 커졌다.**

### 발표에서 쓸 문장

*"같은 밀도에서 비교하면, 냉각률을 4배 바꾸는 것은 체적탄성률을 2 % 움직이는 데 그치지만,
망을 만드는 주체를 BKS 에서 MLIP 로 바꾸면 11 % 움직이고 실험값과의 간극 81 %가 사라진다.
탄성률 오차의 주범은 포텐셜도 냉각률도 아니라 **망 위상을 누가 만들었는가**였다.
다만 같은 조작이 밀도 오차는 +0.8 %에서 −1.8 %로 키운다 — 한쪽을 고치면 다른 쪽이 상한다."*

⚠ 7net 시드 1개. ring·Tg 와 같은 한계.

---

## (기록) 냉각률 성분 분리 — 실행 계획


분석 8 의 위상 형성 효과는 **냉각률 4배 차이가 섞여 있다**(s0 BKS 망 5e12 vs S4 7net 망 2e13).
읽는 포텐셜을 7net 으로 고정하고 두 축을 분리한다:

| 행 | reader | 망 생성자 | 냉각률 | 상태 |
|---|---|---|---|---|
| ① | 7net | BKS | 5e12 | ✅ `s2_relax/ev220_scan.txt` (기존 보고값) |
| ② | 7net | BKS | **2e13** | ❌ 미실행 ← **이게 핵심** |
| ③ | 7net | **7net** | 2e13 | ✅ `ev/ev_s4_7net_scan.txt` |
| ④ | BKS | BKS | 2e13 | ❌ 미실행 (보조, 1분) |

- **[A] 냉각률 효과 = ② − ①** (망 생성자 BKS 로 같고 냉각률만 4배)
- **[B] 위상 형성 효과 = ③ − ②** ← **냉각률까지 맞춘 최종 수치**
- 현재 보고 중인 −5.81 GPa 는 **③ − ①** 이라 [A]+[B] 의 합이다. `[B']` 로 표시해 두었고
  [B] 가 나오면 폐기한다.

예상: [A] 는 작을 것이다. 냉각률 4배 = 0.6 decade 이고 Tg 기울기 180 K/decade 로 환산하면
fictive temperature 가 ~108 K 움직이는 정도다. ring 통계에서도 BKS 5e12↔2e13 차이는
3-ring 3.07 % vs 2.95 % 로 미미했다. **하지만 예상일 뿐이고, [B] 를 주장하려면 재야 한다.**

### 실행

```bash
cd ~/projects/lammps_tutorial/SiO2-MLIP/02_run/s4_mq7net

# ② 7net 이 BKS(2e13) 망을 읽는다 — ~50분. pilot 구간을 반드시 옮길 것
#    (BKS 망의 rho0 ~ 2.2185 -> f0 ~ 0.992. 기본 pilot 0.98~1.04 는 왼쪽 끝에 걸린다)
PILOT_FS="0.96 0.98 1.00 1.02" \
  setsid nohup bash run_ev_s4.sh prod_bks_2e13.data bksnet2e13 > ev_s4_bksnet.log 2>&1 < /dev/null &

# ④ BKS 가 BKS(2e13) 망을 읽는다 — 1분
bash run_ev_bks_s4.sh

# 둘 다 끝나면
python3 ../../04_analysis/src/ev_s4_summary.py
```

`run_ev_s4.sh` 에 `PILOT_FS` 환경변수를 추가했다(기본값은 S4 자체 망용 그대로).
pilot 첫 점의 f 로 기준 부피를 되돌리므로 목록을 바꿔도 정합하다 — 합성 데이터로
재검증했다(첫 점 0.96, 참값 f₀ 0.99166 → 복원 0.99167, ρ₀ 2.2185 정확).

⚠ **④(BKS 행)를 7net 행과 직접 빼지 말 것.** `in.ev_bks_s4` 는 `minimize 0 1e-5 20000`
이고 `in.ev_s4`(7net)는 `0 1e-3 150` 이다 — 7net 이 1스텝 2초라 불가피한 비대칭이고
기존 ev220 vs ev_bks 도 같은 상태였다. **[A]·[B] 는 둘 다 7net 행끼리의 뺄셈이라
영향받지 않는다.**
