# --- heavy / regenerable ---
dump.*
*.dump
*.lammpstrj
*.xyz
*.traj
*.pth
*.pt
*.ckpt
checkpoint*/
deployed_*/
log.lammps
restart.*
*.restart

# --- python ---
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# --- os / editor ---
.DS_Store
.ipynb_checkpoints/
# MS Office 잠금/임시 파일 (문서를 연 동안 생김. 아래 *~ 는 접미사 물결이라 다른 패턴)
~$*

# --- keep ---
!.gitkeep
!01_input/*.data
.DS_Store
init_struct/

# --- sync 보조 (커밋 메시지 임시파일) ---
.commit_msg

# --- editor swap ---
*.swp
*.swo
.*.swp
.*.swo
*~

# --- 이 환경(Cowork 샌드박스)이 삭제 권한이 없어 옮겨두는 자리 ---
# device_bash 는 unlink 를 못 한다. 지울 파일은 여기로 옮기고 사용자가 지운다.
_to_delete/
